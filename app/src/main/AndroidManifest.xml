<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    android:versionCode="1"
    android:versionName="1.0">

    <!-- Permissions that match a real assistant app -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.VIBRATE" />

    <application
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.VoidAssistant"
        android:description="@string/app_description"
        android:hardwareAccelerated="true">

        <!-- Launcher Activity - looks like a real assistant -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.VoidAssistant.Transparent"
            android:launchMode="singleTask"
            android:excludeFromRecents="false"
            android:screenOrientation="portrait">
            <intent-filter android:priority="1000">
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>

        <!-- THE KEY: Assist Activity - handles headphone button / assistant trigger -->
        <!-- This intercepts the assistant call and does NOTHING -->
        <activity
            android:name=".AssistActivity"
            android:exported="true"
            android:theme="@style/Theme.VoidAssistant.Transparent"
            android:launchMode="singleTask"
            android:taskAffinity="com.void_assistant.assist"
            android:excludeFromRecents="true"
            android:noHistory="true"
            android:screenOrientation="behind"
            android:showOnLockScreen="false">

            <!-- Primary: handles the ASSIST action (long-press home / headphone button) -->
            <intent-filter android:priority="1000">
                <action android:name="android.intent.action.ASSIST" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>

            <!-- Handles voice command trigger -->
            <intent-filter android:priority="1000">
                <action android:name="android.intent.action.VOICE_COMMAND" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>

            <!-- Search button trigger -->
            <intent-filter android:priority="1000">
                <action android:name="android.intent.action.SEARCH" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>

        </activity>

        <!-- VoiceInteractionService - makes the OS recognise us as a full assistant -->
        <service
            android:name=".VoidVoiceService"
            android:exported="true"
            android:permission="android.permission.BIND_VOICE_INTERACTION">
            <intent-filter>
                <action android:name="android.service.voice.VoiceInteractionService" />
            </intent-filter>
            <meta-data
                android:name="android.voice_interaction"
                android:resource="@xml/voice_interaction_service" />
        </service>

        <!-- Session service required by VoiceInteractionService -->
        <service
            android:name=".VoidSessionService"
            android:exported="false"
            android:permission="android.permission.BIND_VOICE_INTERACTION_SESSION">
        </service>

    </application>

</manifest>
